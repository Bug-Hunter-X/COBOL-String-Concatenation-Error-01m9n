01  WS-AREA. 
    05  WS-COUNT PIC 9(5) VALUE ZEROS. 
    05  WS-TABLE OCCURS 10 TIMES INDEXED BY WS-INDEX. 
       10  WS-TABLE-ENTRY PIC X(20). 
    05  WS-INDEX-STRING PIC X(2). 

PROCEDURE DIVISION. 
    PERFORM VARYING WS-INDEX FROM 1 BY 1 UNTIL WS-INDEX > 10 
        MOVE WS-INDEX TO WS-INDEX-STRING 
        STRING 'RECORD' DELIMITED BY SIZE 
               WS-INDEX-STRING DELIMITED BY SIZE 
               INTO WS-TABLE(WS-INDEX) 
        END-STRING
    END-PERFORM. 

    DISPLAY 'Table populated successfully'. 
    STOP RUN.